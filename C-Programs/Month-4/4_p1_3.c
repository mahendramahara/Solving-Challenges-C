//WAP in C to for the following logical expression and print the results;
#include <stdio.h>

int main(){
    int i,j,k,l;
    i++ && j++ && k++ || l++;
    printf("i = %d\nj = %d\nk = %d\nl = %d",i,j,k,l);
}